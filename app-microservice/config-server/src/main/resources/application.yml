server:
  port: 8888
spring:
  main:
    banner-mode: off
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/nezirturhalli/config-repo

